1、http转发：ngrok -subdomain 'sub_domain_name' -config 'config_file_path' [-auth 'user:passwd'] 8080
2、tcp转发，需要通过配置文件启动，在配置文件中写入如下配置：
tunnels:
  ssh:
    remote_port: 21000
    proto:
      tcp: 22

其中ssh为通道名称，remote_port为服务器对外暴露端口，proto表示转发协议，tcp: 22表示转发tcp协议22端口的流量。

注意：ngrok配置文件问yml格式的，注意缩进和冒号后的空格
